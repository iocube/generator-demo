angular.module('<%= moduleName %>')
    .directive('<%= directiveName %>', function() {
        return {
            restrict: 'E',
            scope: {},
            link: function(scope, element, attrs, ctrl) {}
        };
    });
